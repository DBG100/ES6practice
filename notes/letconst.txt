1、ES6新增了let命令，用来生命变量，它的用法类似于var，但是所声明的变量只在let命令所在的代码块内有效。
2、for循环的计数器，就很合适使用let命令。
3、let不会出现变量提升。var声明变量的时候会有变量提升的情况出现，即变量在声明前使用（undefined），使用let时在声明前使用变量会直接报错。
4、暂时性死区（暂死区）。块级作用域里使用let声明的变量就“绑定（binding）”在这个区域，不受外界影响。